import { Box, Typography } from "@mui/material";

export const Tickets = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontFamily: "Bold_M" }}>
        Tickets
      </Typography>
      <Typography variant="body1" sx={{ fontFamily: "Regular_M" }}>
        Tickets page content goes here.
      </Typography>
    </Box>
  );
};
